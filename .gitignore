# Dados brutos do INEP (grandes, baixados do site oficial - não versionar)
Dados/
*.zip
*.CSV
*.csv

# Dados tratados: vivem no BigQuery (apmaricato2.inep_ensino_superior), não no git
data/parquet/

# Exports antigos em Excel/Power BI (não são a fonte de verdade do pipeline)
*.xlsx
*.pbix
*.docx

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Ferramentas locais (Claude Code, graphify) - não fazem parte do projeto
.claude/
graphify-out/
